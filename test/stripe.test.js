/**
 * Testing should boot up the firebase functions experiemental and run the functions
 * 
 * https://firebase.google.com/docs/functions/unit-testing
 */
const createStripeApp = require('../src/stripe')

const fakeReq = ()
const fakeRes = ()